# ==========================================
# Defense-Bot 全域環境變數設定檔 (範例)
# ==========================================
# 使用方式：cp .env.example .env，再依據實際環境修改各值

# [API 伺服器對外網址]
# 用途：生成 PPT 下載連結的根網址，也會寫入 FastAPI 的 OpenAPI servers 設定
# 分離網路部署建議直接填可被前端/Dify 存取的位址
# 本地 IP 範例：http://<BACKEND_HOST_OR_IP>:8088
# 對外 IP 範例：https://<BACKEND_PUBLIC_DOMAIN_OR_IP>
SERVER_URL=http://<BACKEND_HOST_OR_IP>
# [Dify Agent 串接設定]
# 前端對話功能 (POST /api/v1/chat) 需要透過此金鑰與 Dify Agent 通訊
# 請至 Dify 應用頁面 > 訪問 API > 複製 API 金鑰後貼入
DIFY_API_KEY=app-XXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Dify Chat API 端點
# 分離網路部署請填 Dify 的可達位址
# 本地 IP 範例：http://<DIFY_HOST_OR_IP>:8080/v1/chat-messages
# 對外 IP 範例：https://<DIFY_PUBLIC_DOMAIN_OR_IP>/v1/chat-messages
# 在 Docker 容器內，localhost 只代表容器自己，請勿使用
# 使用 Dify 雲端服務：https://api.dify.ai/v1/chat-messages
DIFY_API_URL=http://<DIFY_HOST_OR_IP>:8080/v1/chat-messages